[pytest]
addopts = -ra
markers =
    requires_root: test needs raw ICMP sockets and elevated privileges.
    e2e_local: end-to-end ICMP proxy test using a local TCP backend.
    e2e_external: end-to-end ICMP proxy test against an external internet host.
